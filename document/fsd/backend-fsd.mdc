---
alwaysApply: false
---
  endpoints:
  - GET /favorites
  - POST /favorites
  - DELETE /favorites/{appId}
    inputs:
  - appId (string)
    outputs:
  - favorites_list (JSON)
    precondition: 로그인 필요.
    postcondition: 즐겨찾기 변경 사항이 홈화면에 즉시 반영됨.
    nonfunctional:
  - max_favorites <= 10
  - response_time <= 1s
